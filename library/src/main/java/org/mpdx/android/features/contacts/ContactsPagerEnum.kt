package org.mpdx.android.features.contacts

import org.mpdx.android.R

enum class ContactsPagerEnum(val titleResId: Int) {
    INFO(R.string.info),
    DONATION(R.string.donations_lowercase),
    TASKS(R.string.tasks_lowercase),
    NOTES(R.string.notes);
}
