<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="item" type="org.mpdx.android.features.contacts.viewmodel.LinkedInAccountViewModel" />
    </data>

    <TextView
        android:id="@+id/contact_social_media_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        app:drawableStartCompat="@drawable/ic_contact_linkedin_account"
        android:drawablePadding="8dp"
        android:text="@{item.username}"
        android:textColor="@color/primary_gray"
        android:textSize="@dimen/contacts_info_text_size"
        tools:text="@tools:sample/first_names" />

</layout>
