<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data class=".CurrentTaskHeaderItemBinding">
        <import type="androidx.lifecycle.LiveData" />

        <variable
            name="grouping"
            type="org.mpdx.android.features.tasks.tasklist.TaskDueDateGrouping" />
        <variable
            name="listener"
            type="org.mpdx.android.features.tasks.tasklist.CurrentTasksListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/task_header_text"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="@dimen/current_task_item_margin"
            android:layout_marginLeft="@dimen/current_task_item_margin"
            android:layout_weight="1"
            android:gravity="center_vertical|start"
            android:text="@{grouping.stringResource}"
            android:textColor="@color/primary_blue"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="TODAY" />

        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:gravity="center_vertical|end"
            android:onClick="@{() -> listener.toViewAll(grouping)}"
            android:text="@string/tasks_view_all"
            android:textColor="@color/primary_blue"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/task_header_nav_image"/>

        <ImageView
            android:id="@+id/task_header_nav_image"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_marginEnd="@dimen/current_task_item_margin"
            android:layout_marginRight="@dimen/current_task_item_margin"
            android:onClick="@{() -> listener.toViewAll(grouping)}"
            android:src="@drawable/ic_chevron_right"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>

