<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data class=".ContactDonationItemBinding">
        <import type="org.threeten.bp.format.FormatStyle" />

        <variable name="presenter" type="org.mpdx.android.features.contacts.contactdetail.donation.ContactDonationAdapterListener" />
        <variable name="donation" type="org.mpdx.android.features.donations.viewmodel.DonationViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClick="@{() -> presenter.onDonationClicked(donation.model)}"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/donated_amount"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text='@{donation.amount}'
                app:currency="@{donation.currency}" />

            <TextView
                android:id="@+id/donation_date"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="end"
                android:text="@{donation.donationDate}"
                android:textColor="@color/primary_blue"
                app:dateStyle="@{FormatStyle.MEDIUM}"
                app:defaultText="@{@string/contact_no_donation_date}" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:layout_marginBottom="@dimen/contacts_small_text_size"
            android:layout_marginTop="@dimen/contacts_detail_small_textsize"
            android:background="@color/tint_gray"/>
    </LinearLayout>
</layout>
